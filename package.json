{
  "name": "metaagent",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@8.12.1",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test"
  },
  "devDependencies": {
    "turbo": "^1.13.0",
    "typescript": "^5.3.3"
  }
}
