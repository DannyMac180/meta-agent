{
  "name": "{{meta.name}}",
  "version": "{{default meta.version "1.0.0"}}",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx src/mastra/index.ts",
    "build": "tsc -p .",
    "test": "vitest run"
  },
  "dependencies": {
    "@mastra/core": "latest"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.3.3",
    "vitest": "^1.5.0"
  }
}
